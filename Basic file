To implement and run the `GridGainExample` Java class, follow these detailed steps:

### Step 1: Set Up Your Project

If you're using an Integrated Development Environment (IDE) like IntelliJ IDEA or Eclipse, create a new Maven project. Maven will help you manage dependencies and build the project easily.

#### Step 1.1: Create a New Maven Project

1. **In IntelliJ IDEA**:
   - Go to `File -> New -> Project`.
   - Select `Maven` and click `Next`.
   - Specify the project name and location, then click `Finish`.

2. **In Eclipse**:
   - Go to `File -> New -> Project`.
   - Select `Maven Project` and click `Next`.
   - Follow the prompts to configure your project.

#### Step 1.2: Update `pom.xml`

Add the GridGain dependencies to your `pom.xml` file:

```xml
<dependencies>
    <dependency>
        <groupId>org.apache.ignite</groupId>
        <artifactId>ignite-core</artifactId>
        <version>2.14.0</version> <!-- Use the latest version available -->
    </dependency>
    <dependency>
        <groupId>org.apache.ignite</groupId>
        <artifactId>ignite-indexing</artifactId>
        <version>2.14.0</version> <!-- Use the latest version available -->
    </dependency>
</dependencies>
```

### Step 2: Create the Java Class

Create a new Java class named `GridGainExample`.

#### Step 2.1: In IntelliJ IDEA

1. Right-click on `src/main/java` and select `New -> Package`. Name it `com.example.gridgain`.
2. Right-click on the newly created package and select `New -> Java Class`. Name it `GridGainExample`.

#### Step 2.2: In Eclipse

1. Right-click on `src/main/java` and select `New -> Package`. Name it `com.example.gridgain`.
2. Right-click on the newly created package and select `New -> Class`. Name it `GridGainExample`.

### Step 3: Write the Java Code

Copy the provided Java code into the `GridGainExample` class:

```java
package com.example.gridgain;

import org.apache.ignite.Ignite;
import org.apache.ignite.Ignition;
import org.apache.ignite.configuration.CacheConfiguration;
import org.apache.ignite.cache.CacheMode;
import org.apache.ignite.cache.query.SqlFieldsQuery;
import org.apache.ignite.IgniteCache;

public class GridGainExample {
    public static void main(String[] args) {
        // Start an Ignite node.
        try (Ignite ignite = Ignition.start()) {
            // Configure cache.
            CacheConfiguration<Long, String> cacheCfg = new CacheConfiguration<>("myCache");
            cacheCfg.setCacheMode(CacheMode.PARTITIONED);

            // Create or get the cache.
            IgniteCache<Long, String> cache = ignite.getOrCreateCache(cacheCfg);

            // Put data into the cache.
            cache.put(1L, "Hello, GridGain!");
            cache.put(2L, "This is an example.");

            // Retrieve and print the data.
            System.out.println("Cache Entry 1: " + cache.get(1L));
            System.out.println("Cache Entry 2: " + cache.get(2L));

            // Execute an SQL query.
            String sql = "SELECT _key, _val FROM String WHERE _key = ?";
            SqlFieldsQuery sqlQuery = new SqlFieldsQuery(sql).setArgs(1L);

            // Print SQL query results.
            cache.query(sqlQuery).getAll().forEach(row -> 
                System.out.println("SQL Query Result: Key = " + row.get(0) + ", Value = " + row.get(1))
            );
        }
    }
}
```

### Step 4: Build and Run the Project

#### Step 4.1: Using IntelliJ IDEA

1. **Build the Project**:
   - Go to `Build -> Build Project` or press `Ctrl+F9`.

2. **Run the Main Method**:
   - Right-click on the `GridGainExample` class file and select `Run 'GridGainExample.main()'`.

#### Step 4.2: Using Eclipse

1. **Build the Project**:
   - Right-click on the project in the Project Explorer and select `Run As -> Maven build...`.
   - Enter `clean install` in the Goals field and click `Run`.

2. **Run the Main Method**:
   - Right-click on the `GridGainExample` class file and select `Run As -> Java Application`.

### Step 5: Observe the Output

When you run the `GridGainExample` class, you should see the following output in the console:

```
Cache Entry 1: Hello, GridGain!
Cache Entry 2: This is an example.
SQL Query Result: Key = 1, Value = Hello, GridGain!
```

This confirms that the cache was created, data was inserted, retrieved, and queried successfully.
